# 1.0.0 (First Stable Release)

- Mobile Control's variables changed (update to newest variables).
- Added `overlapsUltraComplex()` to touch class.
- Mobile Controls optimized a little bit.
- JoyStick Issues Fixed (Specifically Camera Zoom Issue Fixed).
- Added `mobile_controls_allow_mouse_clicks` for testing mobile controls on pc.

# 0.2.0 (Cancelled Update)

- Added ScreenUtil.
- Added MobileControlManager.
- Added Unique ID (for PsychEngine).
- Improved JoyStick (Now uses the MobileButton).
- TouchUtil & SwipeUtil Classes Removed (use `ScreenUtil.touch`, `ScreenUtil.swipe` instead).

# 0.1.0 (First Release)

- Initial Library